{
  "name": "library",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "babel --copy-files --out-dir dist --ignore *test.js src",
    "test": "nyc mocha tests",
    "watch": "mocha tests --watch",
    "lint": "eslint src",
    "validate": "npm-run-all test lint"
  },
  "author": "Veysel <vyslusta1@gmail.com>",
  "license": "(MIT)",
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.15.4",
    "@babel/core": "^7.15.5",
    "@babel/preset-env": "^7.15.6",
    "chai": "^4.3.4",
    "eslint": "^7.32.0",
    "ghooks": "^2.0.4",
    "mocha": "^9.1.1",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.1.0",
    "rimraf": "^3.0.2"
  },
  "nyc": {
    "reporter": [
      "text",
      "lcov"
    ]
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {
          "targets": "defaults"
        }
      ]
    ]
  }
}
